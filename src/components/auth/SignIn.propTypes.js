import PropTypes from 'prop-types';

export const signInPropTypes = {
    onPageChange: PropTypes.func,
    onClose: PropTypes.func
};

export const signInDefaultProps = {
    onPageChange: undefined,
    onClose: undefined
};
